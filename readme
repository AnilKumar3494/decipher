In an attempt to decrypt the message,

I have followed and used a few steps in msgDecrypt.py

Step 1: converting the entire message into a standard format - removing everything () except the alpabet and then converting